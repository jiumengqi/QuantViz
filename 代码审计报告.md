# QuantViz 量化投资平台 - 源码级代码审计报告

> 审计日期：2026-06-04 | 审计范围：全部源码 | 版本：3.0

---

## 任务一：源码级全面项目总结

### 1. 技术栈全景

| 层级 | 技术 | 版本 | 文件来源 |
|------|------|------|----------|
| **后端框架** | Flask | 未锁定版本 | `requirements.txt` L1 |
| **ORM** | Flask-SQLAlchemy | 未锁定 | `requirements.txt` L2 |
| **数据库** | SQLite 3 | 运行时 | `config.py` L21 |
| **数据库迁移** | Flask-Migrate | 未锁定 | `requirements.txt` L7 |
| **缓存** | Flask-Caching | 未锁定 | `app.py` L64 |
| **认证** | Flask-Login | 未锁定 | `app.py` L76 |
| **数据处理** | pandas, numpy, scipy | 未锁定 | `requirements.txt` L3-5 |
| **可视化** | matplotlib, seaborn | 未锁定 | `requirements.txt` L8-9 |
| **机器学习** | scikit-learn | 未锁定 | `requirements.txt` L10 |
| **数据源** | tushare | 未锁定 | `requirements.txt` L13 |
| **环境管理** | python-dotenv | 未锁定 | `requirements.txt` L14 |
| **HTTP客户端** | requests | 未锁定 | `requirements.txt` L15 |
| **Excel导出** | openpyxl | 未锁定 | `requirements.txt` L16 |
| **PDF导出** | weasyprint | 未锁定 | `requirements.txt` L17 |
| **前端样式** | Tailwind CSS (CDN) | v3.x | 50+模板 `<script src="https://cdn.tailwindcss.com">` |
| **前端图标** | Font Awesome | v4.7.0 + v6.4.0 | 混合使用两个版本 |
| **图表库** | Chart.js | v4.4.8 | 27个模板引入 |
| **图表库2** | ECharts | v5.x | 3个模板引入 |
| **日期选择** | flatpickr | v4.6.13 | `analysis.html` L16-18 |
| **AI服务** | DeepSeek API | v1 | `ai_service.py` L48 |

### 2. 项目目录结构

```
股票分析与量化投资平台1.0/
├── app.py                    # Flask入口，create_app工厂，蓝图注册
├── config.py                 # 三环境配置类（Dev/Prod/Test）
├── db.py                     # SQLAlchemy实例
├── .env                      # 环境变量（⚠️含硬编码密钥）
├── requirements.txt          # 16个依赖（无版本锁定）
├── migrate_database.py       # 数据库迁移脚本
├── create_admin.py           # 管理员创建脚本
│
├── services/                 # 核心业务逻辑
│   ├── auth.py               # ⚠️1541行上帝文件（认证+用户管理+课程+配置+统计）
│   ├── backtest.py           # 回测引擎（635+行核心计算）
│   ├── analyzer.py           # 技术分析器+MockTA
│   ├── data_fetcher.py       # Tushare/akshare数据获取（含大量Mock数据）
│   ├── ai_service.py         # DeepSeek AI对话服务
│   ├── knowledge_base.py     # 知识库静态数据
│   ├── strategy_templates.py # 策略模板定义
│   ├── notification.py       # 通知服务
│   ├── diff_utils.py         # 代码Diff工具
│   ├── export_utils.py       # Excel/CSV/PDF导出（216+198行超长函数）
│   │
│   ├── routes/               # 17个蓝图路由文件
│   │   ├── main.py, api.py, auth.py, backtest.py
│   │   ├── data.py, analysis.py, portfolio.py
│   │   ├── models.py, ml_prediction.py, multi_factor.py
│   │   ├── risk_analysis.py, feedback.py, knowledge.py
│   │   ├── courses.py, notifications.py, community.py
│   │   ├── export.py, site_content_api.py
│   │
│   └── models/               # 数据模型（25个.py文件）
│       ├── user.py           # User模型
│       ├── stock.py          # Strategy, BacktestResult, Portfolio等核心模型
│       ├── community.py      # StrategyShare, Comment, Like
│       ├── feedback.py       # Feedback, ContactMessage
│       ├── notification.py   # Notification
│       ├── courses.py        # Course模型
│       ├── ml_predictor.py   # ML预测模型
│       ├── site_content.py   # 动态站点内容
│       ├── strategy_version.py # 策略版本
│       ├── operation_log.py  # 操作日志
│       ├── pricing/          # Black-Scholes, Bond Pricing
│       ├── portfolio/        # Markowitz, Multi-Factor
│       ├── risk/             # Risk Manager, VaR
│       ├── forecasting/      # ARIMA, GARCH
│       └── performance/      # Attribution
│
├── templates/                # 80个HTML模板（15个子目录）
│   ├── index.html            # 首页
│   ├── components/           # header.html, footer.html
│   ├── auth/                 # 17个认证相关页面
│   ├── backtest/             # 4个回测页面
│   ├── community/            # 3个社区页面
│   ├── portfolio/            # 10个投资组合页面
│   ├── models/               # 7个金融模型页面
│   ├── knowledge/            # 6个知识库页面
│   └── ...
│
├── static/                   # 静态资源
│   ├── css/style.css         # ⚠️2026行（90%为暗色模式代码）
│   └── js/
│       ├── utils.js          # 工具函数（Toast/搜索/主题）
│       ├── charts.js         # ECharts图表封装
│       └── script.js         # 全局脚本
│
└── logs/                     # 日志目录
    └── quant_platform.log    # ⚠️无日志轮转
```

### 3. 核心功能实现

| 功能模块 | 实现状态 | 数据来源 | 关键文件 |
|----------|----------|----------|----------|
| 用户注册/登录 | ✅ 完整 | 真实DB | `services/auth.py` L36-173 |
| 个人中心（头像/密码/设置） | ✅ 完整 | 真实DB | `services/auth.py` L202-1180 |
| 多角色权限（用户/管理员） | ✅ 完整 | 真实DB | `app.py` L75, 各路由`@admin_required` |
| 数据隔离 | ✅ 完善 | `filter_by(user_id=current_user.id)` | 全部路由 |
| 管理员面板 | ✅ 完整 | API从DB获取 | `services/auth.py` L569-1500 |
| 策略回测 | ⚠️ 部分模拟 | 模拟数据回退 | `services/backtest.py` |
| 技术指标分析 | ⚠️ 部分模拟 | analyzer.py mock | `services/analyzer.py` |
| Black-Scholes | ✅ 完整 | 真实计算 | `services/models/pricing/black_scholes.py` |
| Markowitz | ⚠️ 模拟回退 | `np.random.normal` | `services/models/portfolio/markowitz.py` L284 |
| VaR计算 | ✅ 三种方法 | 真实计算+MC模拟 | `services/models/risk/value_at_risk.py` |
| ML预测 | ⚠️ 部分模拟 | mock数据回退 | `services/routes/ml_prediction.py` L72 |
| AI助手 | ✅ 真实API | DeepSeek API | `services/ai_service.py` |
| 多因子分析 | ⚠️ 大量模拟 | `generate_mock_stock_data()` | `services/models/portfolio/multi_factor.py` L573 |
| 知识库 | ✅ 静态内容 | 硬编码字典 | `services/knowledge_base.py` |
| 社区 | ✅ 完整 | 真实DB | `services/routes/community.py` |
| 动态内容管理 | ✅ 完整 | `site_contents`表 | `services/routes/site_content_api.py` |
| 通知系统 | ⚠️ 无实际发送 | DB存储 | `services/notification.py` |
| 数据导出 | ✅ Excel/CSV/PDF | openpyxl/weasyprint | `services/export_utils.py` |

### 4. 代码质量现状

| 指标 | 评级 | 详情 |
|------|------|------|
| **注释覆盖率** | 🟡 中等 | 类和路由函数有docstring，但内部方法注释不完整，中英文混用 |
| **重复代码率** | 🔴 高 | 同一组5只股票代码在**9个文件87+处**重复定义；CRUD try/except模式重复100+次 |
| **模块化程度** | 🟡 中等 | 蓝图分离良好，但`services/auth.py`为1541行上帝文件 |
| **错误处理** | 🔴 存在隐患 | 10处`except: pass`空异常块；5处缺少`db.session.rollback()` |
| **超长函数** | 🔴 需要重构 | 6个函数>100行，最长达216行 |
| **SQL注入风险** | 🔴 严重 | `data_fetcher.py` L164/346，`analyzer.py` L140-149使用f-string拼接SQL |
| **XSS风险** | 🟡 中等 | 6+处`innerHTML`直接插入API数据，未转义 |
| **硬编码** | 🔴 严重 | API密钥硬编码、默认日期`2020-01-01`出现14+次 |

### 5. 依赖与环境

| 问题 | 严重性 | 详情 |
|------|--------|------|
| `requirements.txt`无版本号 | 🟡 | 全部16个依赖未锁定版本，环境不可复现 |
| Tailwind CDN生产使用 | 🔴 | 50+模板使用Play CDN（开发模式，不应生产使用） |
| Font Awesome双版本 | 🟡 | v4.7.0和v6.4.0混用，类名前缀不兼容 |
| Chart.js + ECharts重复加载 | 🟡 | `analysis/heatmap.html`同时加载两个库 |
| 数据库文件散落 | 🟡 | 根目录9个.db文件，`instance/`下另有数据库 |

### 6. 项目核心亮点

1. **完善的分层架构**：`routes/`、`models/`、`services/`三层分离清晰，蓝图注册规范
2. **数据隔离机制**：所有用户数据查询均正确使用`filter_by(user_id=current_user.id)`，安全意识较好
3. **动态内容管理**：`site_contents`表+API实现了无需修改代码即可编辑前端内容的能力
4. **策略版本管理**：`StrategyVersion`模型+diff工具提供了完整的版本对比和回滚功能
5. **全面CRUD分页**：回测、用户、反馈、通知、评论等查询均使用`.paginate()`
6. **模板组件化**：`header.html`和`footer.html`作为公共组件引入

### 7. 现存显性问题

| 编号 | 类型 | 问题描述 | 位置 |
|------|------|----------|------|
| B1 | 🔴致命 | `strategies`表缺少`user_id`列（已通过迁移修复） | `stock.py` Model vs 旧DB |
| B2 | 🔴安全 | `.env`硬编码Tushare+DeepSeek真实API密钥 | `.env` L2, L8 |
| B3 | 🔴安全 | `config.py`硬编码API密钥作为回退值 | `config.py` L44 |
| B4 | 🔴安全 | JWT密钥回退值为`'your-secret-key'` | `api.py` L31 |
| B5 | 🔴安全 | CSRF保护完全禁用 | `app.py` L51-66, `config.py` L58 |
| B6 | 🔴安全 | Debug模式硬编码为True + `host='0.0.0.0'` | `app.py` L291-331 |
| B7 | 🔴SQL注入 | `data_fetcher.py` f-string拼接SQL | `data_fetcher.py` L164, L346 |
| B8 | 🔴SQL注入 | `analyzer.py` f-string拼接SQL | `analyzer.py` L140-149 |
| B9 | 🔴静默异常 | 10处`except: pass`吞没所有错误 | `migrate_database.py` L153-168等 |
| B10 | 🔴无轮转 | `RotatingFileHandler`已导入但未使用 | `app.py` L31 vs L225 |
| B11 | 🟡XSS | 6+处`innerHTML`直接插入API响应数据 | `community/index.html` L164等 |
| B12 | 🟡权限 | `/ai-config`无需登录泄露API密钥前缀 | `main.py` L76 |
| B13 | 🟡权限 | `/debug`和`/debug-html`无需登录 | `main.py` L64, `app.py` L311 |
| B14 | 🟡模拟数据 | 15+API端点返回未标注的模拟金融数据 | `data_fetcher.py`, `portfolio.py`等 |
| B15 | 🟡上帝文件 | `services/auth.py` 1541行需拆分 | `services/auth.py` |
| B16 | 🟡每请求日志 | `before_request`对静态文件也写INFO日志 | `app.py` L295-297 |
| B17 | 🟡图表内存泄漏 | Chart实例未销毁即覆盖 | `index.html` L985等 |
| B18 | 🟢双重载 | `analysis/heatmap.html`同时加载Chart.js+ECharts | `heatmap.html` L12 |

---

## 任务二：分优先级可落地代码修改建议

---

### 🔴 高优先级（必须立即修改，3天内完成）

#### 1. 安全漏洞修复

##### 漏洞1：`.env`文件暴露真实API密钥 — 严重级

**位置**：`.env` L2, L8

**危害**：代码若上传至公开仓库，攻击者可窃取Tushare和DeepSeek API密钥，滥用API配额。

**修复**：

删除`.env`中的真实密钥，创建`.env.example`模板文件：

```diff
# .env (修改后)
- TUSHARE_API_KEY=e527545df5a6c9fd70acae3de622c0513f624f710393e27a44aa4e84
+ TUSHARE_API_KEY=your_tushare_api_key_here
- DEEPSEEK_API_KEY=sk-8b65fc7617764b9aabe95038af72281c
+ DEEPSEEK_API_KEY=your_deepseek_api_key_here
```

同时创建`.gitignore`添加`.env`（如未包含）。

**验证**：删除`.env`后重启应用，确认报错提示"请配置API密钥"而非使用硬编码回退值。

---

##### 漏洞2：`config.py`硬编码API密钥回退值 — 严重级

**位置**：`config.py` L44, `api.py` L31

**危害**：即使环境变量未设置，系统也以硬编码密钥运行，绕过安全机制。

**修复**：

```diff
# config.py L44
- TUSHARE_API_KEY = os.environ.get('TUSHARE_API_KEY') or 'e527545df5a6c9fd70acae3de622c0513f624f710393e27a44aa4e84'
+ TUSHARE_API_KEY = os.environ.get('TUSHARE_API_KEY')
+ if not TUSHARE_API_KEY:
+     raise ValueError("TUSHARE_API_KEY 未配置，请在 .env 文件中设置")

# api.py L31
- JWT_SECRET_KEY = os.environ.get('JWT_SECRET_KEY') or 'your-secret-key'
+ JWT_SECRET_KEY = os.environ.get('JWT_SECRET_KEY')
+ if not JWT_SECRET_KEY:
+     raise ValueError("JWT_SECRET_KEY 未配置，请在 .env 文件中设置")
```

---

##### 漏洞3：CSRF保护完全禁用 — 严重级

**位置**：`app.py` L51-66, `config.py` L58

**危害**：所有POST请求无CSRF令牌保护，攻击者可构造恶意页面诱导已登录用户执行非预期操作。

**修复**：

```diff
# config.py L58
- WTF_CSRF_ENABLED = False
+ WTF_CSRF_ENABLED = True

# app.py L51-66 (取消注释)
- # from flask_wtf.csrf import CSRFProtect
- # csrf = CSRFProtect()
+ from flask_wtf.csrf import CSRFProtect
+ csrf = CSRFProtect()
- # csrf.init_app(app)
+ csrf.init_app(app)
```

然后在所有POST表单中添加`<input type="hidden" name="csrf_token" value="{{ csrf_token() }}">`（需安装`flask-wtf`依赖）。

---

##### 漏洞4：Debug模式硬编码 + `host='0.0.0.0'` — 严重级

**位置**：`app.py` L291-292, L327-331

**危害**：生产环境Debug模式启用会暴露源码、交互式Python shell（Werkzeug调试器），`host=0.0.0.0`暴露给局域网。

**修复**：

```diff
# app.py L291-292
- from config import DevelopmentConfig
- app = create_app(DevelopmentConfig)
+ config_name = os.environ.get('FLASK_CONFIG') or 'development'
+ import config
+ app_config = getattr(config, 
+     {'development': 'DevelopmentConfig',
+      'production': 'ProductionConfig',
+      'testing': 'TestingConfig'}.get(config_name, 'DevelopmentConfig'))
+ app = create_app(app_config)

# app.py L327-331
- app.run(debug=app.config.get('DEBUG', False), port=..., host='0.0.0.0', ...)
+ app.run(
+     debug=app.config.get('DEBUG', False),
+     port=app.config.get('PORT', 5000),
+     host=app.config.get('HOST', '127.0.0.1'),
+     use_reloader=app.config.get('DEBUG', False)
+ )
```

---

#### 2. 致命Bug修复

##### Bug1：SQL注入 — f-string拼接SQL（`data_fetcher.py`）

**位置**：`data_fetcher.py` L164, L346

**复现**：若`ts_code`来自用户输入（如API参数）且包含`'; DROP TABLE--`，将导致SQL注入。

**修复**：

```diff
# data_fetcher.py L164
- query = f"SELECT * FROM {table_name} WHERE ts_code = '{ts_code}' AND trade_date >= '{start_date}' AND trade_date <= '{end_date}'"
+ query = "SELECT * FROM {} WHERE ts_code = ? AND trade_date >= ? AND trade_date <= ?".format(table_name)
+ cursor.execute(query, (ts_code, start_date, end_date))

# data_fetcher.py L346
- query = f"SELECT * FROM {table_name} WHERE ts_code = '{ts_code}' AND trade_date >= '{start_date}' AND trade_date <= '{end_date}'"
+ query = "SELECT * FROM {} WHERE ts_code = ? AND trade_date >= ? AND trade_date <= ?".format(table_name)
+ cursor.execute(query, (ts_code, start_date, end_date))
```

---

##### Bug2：SQL注入 — f-string拼接SQL（`analyzer.py`）

**位置**：`analyzer.py` L140-149

**修复**：同样改为参数化查询：

```diff
# analyzer.py L140-149
- query = f"SELECT * FROM stock_daily WHERE ts_code = '{ts_code}'"
- conditions.append(f"trade_date >= '{start_date}'")
- conditions.append(f"trade_date <= '{end_date}'")
- query += " AND " + " AND ".join(conditions)
+ query = "SELECT * FROM stock_daily WHERE ts_code = ?"
+ params = [ts_code]
+ if start_date:
+     query += " AND trade_date >= ?"
+     params.append(start_date)
+ if end_date:
+     query += " AND trade_date <= ?"
+     params.append(end_date)
+ df = pd.read_sql_query(query, conn, params=params)
```

---

##### Bug3：静默异常吞没 — `except: pass`（`migrate_database.py`）

**位置**：`migrate_database.py` L153, L158, L163, L168, L201

**危害**：迁移脚本中ALTER TABLE失败时静默忽略，导致表结构不完整但无任何提示。

**修复**：

```diff
# migrate_database.py L153 (示例，5处同样修改)
  try:
      cursor.execute("ALTER TABLE strategies ADD COLUMN user_id INTEGER")
  except:
-     pass
+     current_app.logger.warning(f"无法添加 strategies.user_id 列（可能已存在）: {traceback.format_exc()}")
```

---

##### Bug4：每请求日志对静态文件也写入 — 性能反模式

**位置**：`app.py` L295-297

**修复**：

```diff
# app.py L295-297
  @app.before_request
  def log_request():
+     if request.path.startswith('/static/'):
+         return  # 跳过静态文件请求的日志
      app.logger.info(f"Request: {request.method} {request.url}")
```

---

#### 3. 核心功能缺陷

##### 缺陷1：未使用`RotatingFileHandler`，日志无限增长

**位置**：`app.py` L31, L225

**修复**：

```diff
# app.py L225
- file_handler = logging.FileHandler('logs/quant_platform.log', mode='a', encoding='utf-8')
+ file_handler = RotatingFileHandler(
+     'logs/quant_platform.log', maxBytes=10*1024*1024, backupCount=5,
+     mode='a', encoding='utf-8'
+ )
```

---

##### 缺陷2：模拟数据无标识，误导金融决策

**位置**：`data_fetcher.py` L224-271等15+处

**修复**：在所有模拟数据返回时添加`_is_simulated`标记：

```diff
# data_fetcher.py _generate_mock_stock_data() 末尾
  df = pd.DataFrame(data)
+ df.attrs['is_simulated'] = True
  return df
```

同时在所有模板中显示"（模拟数据）"警告。

---

### 🟡 中优先级（1周内完成）

#### 1. 代码质量优化

##### 提取重复的股票代码定义

**位置**：9个文件中87+处重复定义

**修复**：创建`config.py`中的`HOT_STOCKS`常量：

```python
# config.py 新增
HOT_STOCKS = [
    {'code': '600036.SH', 'name': '招商银行'},
    {'code': '000858.SZ', 'name': '五粮液'},
    {'code': '601318.SH', 'name': '中国平安'},
    {'code': '600519.SH', 'name': '贵州茅台'},
    {'code': '000001.SZ', 'name': '平安银行'},
]
```

然后在`data.py` L17-23、`backtest.py` L33-35、`analysis.py` L18-22等所有位置改为`from config import HOT_STOCKS`。

---

##### 拆分`services/auth.py`上帝文件

**位置**：`services/auth.py`（1541行）

**修复方案**：按职责拆分为以下文件：

| 新文件 | 包含内容 | 行数范围 |
|--------|----------|----------|
| `routes/auth.py` | 登录/注册/注销/密码重置路由 | L36-280 |
| `routes/profile.py` | 个人中心/编辑资料/头像上传 | L280-500 |
| `routes/admin_users.py` | 用户管理API | L569-722, L1310-1500 |
| `routes/admin_courses.py` | 课程管理 | L698-969 |
| `routes/admin_config.py` | 系统配置管理 | L1103-1253 |
| `services/user_service.py` | 用户业务逻辑 | 提取L36-173的业务逻辑 |

---

##### 统一注释语言

**修复**：全项目统一使用中文docstring + Google风格参数说明（`:param:` `:return:`）。

---

#### 2. 性能优化

##### 替换Tailwind CDN为构建版本

**位置**：50+个模板中的`<script src="https://cdn.tailwindcss.com">`

**修复**：

```bash
npm install -D tailwindcss postcss autoprefixer
npx tailwindcss init
```

配置`tailwind.config.js`扫描所有`templates/**/*.html`，生成静态CSS至`static/css/tailwind.min.css`，替换所有模板中的CDN脚本。

---

##### 缓存热点数据

**位置**：`data_fetcher.py`, `strategy_templates.py`, `knowledge_base.py`

**修复**：

```python
# app.py init_cache() 扩展
from services.strategy_templates import get_all_templates
from services.knowledge_base import get_knowledge_data

with app.app_context():
    cache.set('strategy_templates', get_all_templates(), timeout=3600)
    cache.set('knowledge_base', get_knowledge_data(), timeout=3600)
    cache.set('hot_stocks', app.config['HOT_STOCKS'], timeout=86400)
```

---

##### `analysis/heatmap.html`中删减Chart.js

**位置**：`templates/analysis/heatmap.html` L12

**修复**：该页面已使用ECharts，移除Chart.js引用。

---

#### 3. 架构提升

##### 错误处理统一化

**位置**：全局100+处`except Exception as e`

**修复**：创建装饰器：

```python
# services/decorators.py 新增
def api_error_handler(func):
    @wraps(func)
    def wrapper(*args, **kwargs):
        try:
            return func(*args, **kwargs)
        except ValueError as e:
            return jsonify({'error': str(e)}), 400
        except PermissionError as e:
            return jsonify({'error': str(e)}), 403
        except Exception as e:
            db.session.rollback()
            current_app.logger.error(f"[{func.__name__}] {e}", exc_info=True)
            return jsonify({'error': '服务器内部错误'}), 500
    return wrapper
```

---

### 🟢 低优先级（时间充裕时完成）

#### 1. 用户体验优化

- 使用`textContent`替代部分`innerHTML`预防XSS
- 添加骨架屏加载效果
- 响应式适配手机端

#### 2. 功能扩展建议

1. **回测参数网格搜索**：基于现有回测引擎，添加Grid Search CV
2. **实时推送**：集成WebSocket（Flask-SocketIO）实现实时行情推送
3. **邮件通知**：补充注册验证、密码重置、回测完成邮件（已有SMTP配置框架）
4. **Docker化**：添加`Dockerfile`和`docker-compose.yml`
5. **单元测试**：为回测计算逻辑添加pytest用例

#### 3. 技术债务清理

- 清理根目录9个冗余数据库文件，统一到`instance/`目录
- 删除`templates/debug.html`和`/debug-html`端点
- 统一Font Awesome为单一版本（推荐v6）
- 移除未使用的`lru_cache`导入（`data_fetcher.py` L11）

---

## 🔥 3天紧急修复清单

| 优先级 | 任务 | 文件 | 预计耗时 |
|--------|------|------|----------|
| **1** | 删除`.env`中的真实API密钥，创建`.env.example` | `.env` | 10min |
| **2** | 移除`config.py`和`api.py`中的硬编码回退密钥 | `config.py` L44, `api.py` L31 | 10min |
| **3** | 修复`data_fetcher.py`和`analyzer.py`的f-string SQL注入 | `data_fetcher.py` L164/346, `analyzer.py` L140 | 30min |
| **4** | 修复`migrate_database.py` 5处`except: pass` | `migrate_database.py` L153-201 | 10min |
| **5** | 关闭Debug模式或改用环境变量控制 | `app.py` L291-331 | 20min |
| **6** | 为`/ai-config`、`/debug`、`/community/create`添加`@login_required` | `main.py`, `community.py` | 15min |
| **7** | 使用`RotatingFileHandler`替换`FileHandler` | `app.py` L225 | 5min |

**总计预估：约2小时**

---

> **报告生成时间**：2026-06-04  
> **审计工具**：全源码人工扫描 + AI辅助分析  
> **审计覆盖率**：58个.py文件 + 80个HTML模板 + 3个JS文件 = 100%
